# Copy to .env.development / .env.production and adjust.
# Backend API base URL including the /api prefix.
# PLACEHOLDER - replace with the real deployment URL. In dev, it defaults to http://localhost:3000/api if unset.
VITE_API_BASE_URL=https://api.trab.go.tz/api
VITE_APP_NAME=IDRAS · TRA Appeals Portal
